# ─── Docker Compose environment ──────────────────────────────────────────────
# Copy this to .env at the repo root to override Docker Compose defaults.

# PostgreSQL
POSTGRES_USER=revio
POSTGRES_PASSWORD=revio
POSTGRES_DB=revio

# API
LOG_LEVEL=INFO
DEBUG=false
