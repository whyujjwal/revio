# openapi-gen

Exports the OpenAPI schema from FastAPI to `openapi.json` at the repo root.

## When to use
After adding or modifying API routes or Pydantic schemas. If you also need TypeScript types updated, use `type-sync` instead.

## Usage
```bash
bash skills/openapi-gen/run.sh
```
